#ifndef MENU_H
#define MENU_H

/**
 * A function to display the main menu
 * 
 * @return The selected option
 */
int main_menu();

/**
 * A function to display the second level menu
 * 
 * @return The selected option
 */
int second_level_menu();

/**
 * A function to display the third level menu
 * 
 * @return The selected option
 */
int third_level_menu();

/**
 * A function to display the choose player color menu
 * 
 * @return The selected option
 */
int choose_player_color();

/**
 * A function to display the AI settings menu
 * 
 * @return The selected option
 */
int ai_settings_menu();

/**
 * A function to display the pawn promotion menu
 * 
 * @return The piece after promotion
 */

/**
 * A function to display the pawn promotion menu
 * 
 * @return The piece after promotion
 */
char promote_pawn();

#endif

/* EOF */